.DS_Store
.claude/*.local.md
node_modules/
dist/
coverage/

# Confidential — proving-project process record (.local/LOG.md), kept local only
/.local/


# Dogfood/demo project artifacts at the repo root (real projects live
# in their own repos; these appear here only when running auto-fde on itself)
/dashboard.html
/site.html
/.build/
/feedback/
/test/
__pycache__/
*.pyc

# Workflow-agent worktrees (session debris)
.claude/worktrees/
